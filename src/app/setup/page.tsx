export default function SetupPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p>Setup page placeholder — login worked, first-time user.</p>
    </main>
  )
}
